# Rotor environment variables
# Copy to .env and fill in values

# GitHub token — optional if `gh` CLI is authenticated (preferred)
# GITHUB_TOKEN=ghp_...

# Override Ollama URL (default: http://localhost:11434)
# OLLAMA_URL=http://localhost:11434
